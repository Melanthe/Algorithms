	bool checkway(node *leaf, node *kink, std::vector<node *> &way)
	{
		node *current = leaf;
		bool result = false;
	
		while (current != root)
		{
			way.push_back(current);
			if (current == kink)
			{
				result = true;
				break;
			}
			current = current->father;
		}
		return result;
	}